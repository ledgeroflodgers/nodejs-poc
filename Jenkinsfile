pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello Node JS From ledger of lodgers"'
                sh '''
                    echo "Multiline shell steps works too"
                    ls -lah
                '''
            }
        }
    }
}
